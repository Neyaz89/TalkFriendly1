# Types

All TypeScript interfaces and types are defined here — one file per domain.

Exported from `index.ts` for clean single-import usage:

```ts
import type { UserProfile, MoodEntry, JournalEntry } from "@/types";
```
